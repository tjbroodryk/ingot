# dependencies
node_modules/

# build output
dist/
build/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# coverage
coverage/

# @ingot/server's base tier when no bucket is configured — Parquet it wrote
# itself, reproducible by re-running whatever produced it.
.ingot-data/

# @ingot/bench's transcripts and reports. A published result belongs in the
# site or in a release, not committed from whoever happened to run it last.
packages/bench/results/

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
