import { DeploymentPage, deploymentMetadata } from '../../deployment/deployment-page';

/** `/deployment`, in a landing build only. */
export const metadata = deploymentMetadata;

export default DeploymentPage;
