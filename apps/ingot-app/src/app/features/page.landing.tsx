import { FeaturesPage, featuresMetadata } from '../../features/features-page';

/**
 * `/features` in a landing build.
 *
 * Landing only: a dashboard build's reader has the reference and a running
 * service, and is past being told what it can do.
 */
export const metadata = featuresMetadata;

export default FeaturesPage;
