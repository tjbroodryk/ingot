import { WhyPage, whyMetadata } from '../../why/why-page';

/**
 * `/why` in a landing build.
 *
 * Landing only, and for the reason `/deployment` is: a dashboard build is the
 * site that ships beside a running service, and the argument for running one
 * has already been had by whoever did.
 */
export const metadata = whyMetadata;

export default WhyPage;
